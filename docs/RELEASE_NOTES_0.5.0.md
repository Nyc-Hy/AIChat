# AIChat v0.5.0 Release Notes

AIChat v0.5.0 is the first CLI/TUI-focused release candidate for a cross-platform Vibe Coding assistant.

The main goal of this release is to make AIChat usable from the cross-platform CLI/TUI, with conservative defaults for accurate task execution, lower token usage, and predictable tool approval.

## Highlights

- New cross-platform `aichat` CLI.
- New `aichat tui` interactive session mode.
- Fast / Standard / Deep execution modes.
- First-class DeepSeek, MiMo, and MiniMAX model profiles.
- Conservative default agent settings to reduce token use and avoid unnecessary planner/sub-agent flows.
- Legacy Windows-only WPF shell kept out of the cross-platform path.
- Release packages for:
  - `osx-arm64`
  - `linux-x64`
  - `win-x64`
- SHA-256 checksum files for release assets.

## CLI Commands

- `aichat --version`
- `aichat doctor`
- `aichat models`
- `aichat config show`
- `aichat config list`
- `aichat config use`
- `aichat config set-provider`
- `aichat projects list`
- `aichat init`
- `aichat ask`
- `aichat tui`

## Verification

Validated locally on Windows:

- Build passes with 0 warnings and 0 errors.
- Test suite passes: 563 tests.
- `win-x64` package runs `--version`, `models`, `config set-provider`, `init`, `projects list`, `doctor`, and TUI command switching.
- `osx-arm64` and `linux-x64` packages are generated and structurally verified on Windows.

Before marking the release fully verified, run smoke tests on macOS Apple Silicon and Linux x64:

```bash
./aichat --version
./aichat doctor
./aichat tui --project <repo>
```

## Checksums

Use the `.sha256` files from GitHub Release artifacts, or the local `artifacts/release/SHA256SUMS.txt` file generated by:

```powershell
pwsh scripts/publish-cli.ps1
```
